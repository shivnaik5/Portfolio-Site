# Copy to studio/.env and fill in from https://sanity.io/manage
#
# The Sanity CLI reads env files from this directory, not the repo root, and only
# exposes variables prefixed with SANITY_STUDIO_. These duplicate the values in
# the root .env by design.

SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
